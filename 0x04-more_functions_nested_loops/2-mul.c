#include "main.h"
/**
 * mul - function for multiplication of tow intgers
 * @a: the first intger in the function
 * @b: the second intger in the function
 * Return: the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
